Input (224x224x3)
│
├── Conv2d(3 → 64, kernel=7x7, stride=2, padding=3)   → 112x112x64
├── MaxPool(3x3, stride=2, padding=1)                 → 56x56x64
│
├── Conv2d(64 → 64, kernel=1x1)
├── Conv2d(64 → 192, kernel=3x3, padding=1)           → 56x56x192
├── MaxPool(3x3, stride=2, padding=1)                 → 28x28x192
│
├── Inception(192 → [64, (96,128), (16,32), 32])      → 28x28x256
├── Inception(256 → [128, (128,192), (32,96), 64])    → 28x28x480
├── MaxPool(3x3, stride=2, padding=1)                 → 14x14x480
│
│   ┌──────────────────────────────────────┐
│   │ **Auxiliary Classifier 1**          │
│   │ (InceptionAux, in_channels=480)     │
│   │ Giúp giảm vanishing gradient        │
│   └──────────────────────────────────────┘
=> Average Pooling (kernel 5×5, stride 3)
=> 1×1 Convolution (128 filters)
=> Flatten
=> Fully Connected (1024 units)
=> Dropout (0.7)
=> Fully Connected (num_classes)
=> Softmax (chỉ dùng khi training)
│
├── Inception(480 → [192, (96,208), (16,48), 64])     → 14x14x512
├── Inception(512 → [160, (112,224), (24,64), 64])    → 14x14x512
├── Inception(512 → [128, (128,256), (24,64), 64])    → 14x14x512
├── Inception(512 → [112, (144,288), (32,64), 64])    → 14x14x528
│
│   ┌──────────────────────────────────────┐
│   │ **Auxiliary Classifier 2**          │
│   │ (InceptionAux, in_channels=528)     │
│   └──────────────────────────────────────┘
│
├── Inception(528 → [256, (160,320), (32,128), 128])  → 14x14x832
├── MaxPool(3x3, stride=2, padding=1)                 → 7x7x832
├── Inception(832 → [256, (160,320), (32,128), 128])  → 7x7x832
├── Inception(832 → [384, (192,384), (48,128), 128])  → 7x7x1024
│
├── GlobalAvgPool(7x7)                                → 1x1x1024
├── Dropout(0.4)
├── Linear(1024 → 1000)
└── Softmax